{% extends 'base.html.twig' %}

{% block title %}Formulaire{% endblock %}
{% block body %}
    <div style="max-width: 300px">
    <h4>Création d'un tweet</h4>
    <div style="margin-top: 15px; margin-bottom: 15px">
        {{ form_start(form) }}
            {{ form_row(form.content, {
                'label': false,
                'attr': {'class': 'form-control'}
            }) }}
            {{ form_row(form.submit,{'attr': {'class': 'form-control'}}) }}
        {{ form_end(form) }}
    </div>
    </div>
    <div class="card"  style="margin-bottom: 15px">
        <div class="card-body">
            <h6 class="card-title">id #{{ lastTweet.id }}</h6>
            <p class="card-text">{{ lastTweet.content }}</p>
            <a href="{{ path('tweet.edit', { id: lastTweet.id}) }}" class="btn btn-info">Modifier</a>
            <a href="{{ path('tweet.delete', { id: lastTweet.id}) }}" class="btn btn-warning">Supprimer</a>
        </div>
    </div>
{% endblock %}